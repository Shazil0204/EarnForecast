import { StyleSheet, Text, View } from "react-native";

const Slogan = () => {
  return (
    <View>
      <Text>See Tomorrow’s Income Today</Text>
    </View>
  );
};

export default Slogan;

const styles = StyleSheet.create({});
